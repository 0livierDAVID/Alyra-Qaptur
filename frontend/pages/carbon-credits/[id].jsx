import { Inter } from "next/font/google";
import Layout from "@/components/Layout";

const inter = Inter({ subsets: ["latin"] });

export default function CarbonCredit() {
  return <Layout>Carbon credit on sale item page</Layout>;
}
